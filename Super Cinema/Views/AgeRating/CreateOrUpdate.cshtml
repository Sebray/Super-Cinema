@model Buisness.Interop.Data.AgeRatingDto

@{
    ViewData["Title"] = "Возрастной рейтинг";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateOrUpdate">
            <input type="hidden" value="@(Model is null ? 0 : Model.Id)" name="Id" />

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Rating" class="control-label">Название:</label>
                <input asp-for="Rating" maxlength="60" size="22" placeholder="Например, 0+, 12+" />
                <span class="text-danger" asp-validation-for="@Model.Rating"></span>
            </div>

            <div class="form-group">
                <input type="submit" id="SubmitButton" value="Сохранить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>